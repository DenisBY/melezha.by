<div class="span14">
		<table>
		<% @companies_all.each do |company| %>
			<tr>
	       		<td><a href="/?q=<%= h(company.title) %>"><%= company.title %></a></td>
	       	</tr>
	    <% end %>
	    </table>
    <strong>Всего Компаний:</strong> <%= @companies_total.id %>
</div>
